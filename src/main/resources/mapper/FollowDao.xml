<?xml version="1.0" encoding="UTF-8"?><!DOCTYPE mapper  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"  "http://mybatis.org/dtd/mybatis-3-mapper.dtd"><mapper namespace="com.yskj.dao.FollowDao" ><!-- 查询字段 --><sql id="columnList" >	f.id,f.zw,f.zwtimes,	 f.openID, f.createBy, f.createTime, f.updateBy, f.updateTime, f.version, f.isDeleted</sql><!--通过id集合in查询--><sql id="inClause">    <if test="column!=null">        <if test="ids==null">            and 1=0        </if>        <if test="ids!=null">            <if test="ids.size>0">                and  f.${column} in                <foreach item="items" index="index" collection="ids" open="(" separator="," close=")">                    #{items}                </foreach>            </if>            <if test="ids.size==0">                and 1=0            </if>        </if>    </if></sql><!-- 查询条件 --><sql id="whereClause">where 1=1<trim  suffixOverrides="," >	<if test="condition.id != null" >	    and f.id = #{condition.id}	</if>	<if test="condition.openID != null" >and  f.openID = #{condition.openID}</if><if test="condition.createBy != null" >and  f.createBy = #{condition.createBy}</if><if test="condition.createTime != null" >and  f.createTime = #{condition.createTime}</if><if test="condition.updateBy != null" >and  f.updateBy = #{condition.updateBy}</if><if test="condition.updateTime != null" >and  f.updateTime = #{condition.updateTime}</if><if test="condition.version != null" >and  f.version = #{condition.version}</if><if test="condition.isDeleted != null" >and  f.isDeleted = #{condition.isDeleted}</if>	<if test="condition.zw != null" >and  f.zw = #{condition.zw}</if></trim><include refid="inClause"/></sql><!-- 模糊查询条件 --><sql id="whereLikeClause">where 1=1<trim  suffixOverrides="," >	<if test="condition.id != null" >	    and f.id = #{condition.id}	</if>													 			<if test="condition.openID != null" >and  UPPER(f.openID) LIKE   UPPER(CONCAT(CONCAT('%',#{condition.openID}),'%'))</if><if test="condition.createBy != null" >and  UPPER(f.createBy) LIKE   UPPER(CONCAT(CONCAT('%',#{condition.createBy}),'%'))</if><if test="condition.createTime != null" >and  UPPER(f.createTime) LIKE   UPPER(CONCAT(CONCAT('%',#{condition.createTime}),'%'))</if><if test="condition.updateBy != null" >and  UPPER(f.updateBy) LIKE   UPPER(CONCAT(CONCAT('%',#{condition.updateBy}),'%'))</if><if test="condition.updateTime != null" >and  UPPER(f.updateTime) LIKE   UPPER(CONCAT(CONCAT('%',#{condition.updateTime}),'%'))</if><if test="condition.version != null" >and  UPPER(f.version) LIKE   UPPER(CONCAT(CONCAT('%',#{condition.version}),'%'))</if><if test="condition.isDeleted != null" >and  UPPER(f.isDeleted) LIKE   UPPER(CONCAT(CONCAT('%',#{condition.isDeleted}),'%'))</if></trim><include refid="inClause"/></sql><!-- 插入记录 --><insert id="add"  >  insert into follow (id,zw,zwtimes,openID,createBy,createTime,updateBy,updateTime,version,isDeleted)  values(#{id,jdbcType=VARCHAR},#{zw},#{zwtimes},#{openID},#{createBy},#{createTime},#{updateBy},#{updateTime},#{version},#{isDeleted})</insert><!-- 修改记录，只修改只不为空的字段 --><update id="update" >	update follow set	<trim  suffixOverrides="," >	<if test="openID != null ">openID=#{openID},</if><if test="createBy != null ">createBy=#{createBy},</if><if test="createTime != null ">createTime=#{createTime},</if><if test="updateBy != null ">updateBy=#{updateBy},</if><if test="updateTime != null ">updateTime=#{updateTime},</if><if test="version != null ">version=#{version},</if><if test="isDeleted != null ">isDeleted=#{isDeleted},</if>		<if test="zw != null ">zw=#{zw},</if>		<if test="zwtimes != null ">zwtimes=#{zwtimes},</if>	</trim> where id=#{id} and version = #{version}-1</update><!-- 删除记录 --><delete id="del" >	delete from follow where id = #{id}</delete><!-- 根据id查询  --><select id="get"   resultType="Follow"  parameterType="String">	select <include refid="columnList" /> 	from follow  f	where  f.id = #{id}  and f.isDeleted = false</select><!-- 列表总数--><select id="count" resultType="java.lang.Long" >	select count(1) 	from follow  f	<include refid="whereClause"/></select><!-- 查询page --><select id="findPage" resultType="Follow" >	select 	<include refid="columnList"/>	from follow f	<include refid="whereClause"/>	<if test="orderByClause!=null">${orderByClause}</if></select><!-- 模糊查询page --><select id="findLikePage" resultType="Follow" >	select 	<include refid="columnList"/>	from follow f	<include refid="whereLikeClause"/>	<if test="orderByClause!=null">${orderByClause}</if></select><!-- 查询列表list --><select id="findList" resultType="Follow" >	select	<include refid="columnList"/>	from follow f	<include refid="whereClause"/>	<if test="condition.isvalid!=null">		and (f.zwtimes    &lt;   3 or f.zwtimes is null)        and f.zw = true	</if>	<if test="orderByClause!=null">${orderByClause}</if></select><!--模糊查询list--><select id="findLikeList" resultType="Follow" >	select	<include refid="columnList"/>	from follow f	<include refid="whereLikeClause"/>	<if test="orderByClause!=null">${orderByClause}</if></select><!-- 查询符合条件的数据的id集合 --><select id="findIds" resultType="String">	select f.${idsColumn}	from follow f    <include refid="whereClause"/>    <if test="orderByClause!=null">${orderByClause}</if></select><!-- 查询符合模糊条件的数据的id集合 --><select id="findLikeIds" resultType="String">	select f.${idsColumn}	from follow f    <include refid="whereLikeClause"/>    <if test="orderByClause!=null">${orderByClause}</if></select><!-- 获取一个对象通过非id --><select id="one" resultType="Follow" >     select    <include refid="columnList"/>     from follow f    <include refid="whereClause"/>    <if test="orderByClause!=null">${orderByClause}</if>    limit 0,1</select><!--如果你想返回一个HashMap 就实现这个select标签--><select id="queryHashMap" resultType="java.util.HashMap"  parameterType="Object"></select>	<update id="clearTimes">		update follow set zwtimes = 0	</update></mapper>