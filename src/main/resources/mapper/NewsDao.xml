<?xml version="1.0" encoding="UTF-8"?><!DOCTYPE mapper  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"  "http://mybatis.org/dtd/mybatis-3-mapper.dtd"><mapper namespace="com.yskj.dao.NewsDao" ><!-- 查询字段 --><sql id="columnList" >	n.id,	 n.title, n.content, n.createBy, n.createTime, n.updateBy, n.updateTime, n.version, n.isDeleted, n.description,u.nickName as 'author'</sql><!--通过id集合in查询--><sql id="inClause">    <if test="column!=null">        <if test="ids==null">            and 1=0        </if>        <if test="ids!=null">            <if test="ids.size>0">                and  n.${column} in                <foreach item="items" index="index" collection="ids" open="(" separator="," close=")">                    #{items}                </foreach>            </if>            <if test="ids.size==0">                and 1=0            </if>        </if>    </if></sql><!-- 查询条件 --><sql id="whereClause">where 1=1<trim  suffixOverrides="," >	<if test="condition.id != null" >	    and n.id = #{condition.id}	</if>	<if test="condition.title != null" >and  n.title = #{condition.title}</if><if test="condition.content != null" >and  n.content = #{condition.content}</if><if test="condition.createBy != null" >and  n.createBy = #{condition.createBy}</if><if test="condition.createTime != null" >and  n.createTime = #{condition.createTime}</if><if test="condition.updateBy != null" >and  n.updateBy = #{condition.updateBy}</if><if test="condition.updateTime != null" >and  n.updateTime = #{condition.updateTime}</if><if test="condition.version != null" >and  n.version = #{condition.version}</if><if test="condition.isDeleted != null" >and  n.isDeleted = #{condition.isDeleted}</if><if test="condition.description != null" >and  n.description = #{condition.description}</if></trim><include refid="inClause"/></sql><!-- 模糊查询条件 --><sql id="whereLikeClause">where 1=1<trim  suffixOverrides="," >	<if test="condition.id != null" >	    and n.id = #{condition.id}	</if>													 			<if test="condition.title != null" >and  UPPER(n.title) LIKE   UPPER(CONCAT(CONCAT('%',#{condition.title}),'%'))</if><if test="condition.content != null" >and  UPPER(n.content) LIKE   UPPER(CONCAT(CONCAT('%',#{condition.content}),'%'))</if><if test="condition.createBy != null" >and  UPPER(n.createBy) LIKE   UPPER(CONCAT(CONCAT('%',#{condition.createBy}),'%'))</if><if test="condition.createTime != null" >and  UPPER(n.createTime) LIKE   UPPER(CONCAT(CONCAT('%',#{condition.createTime}),'%'))</if><if test="condition.updateBy != null" >and  UPPER(n.updateBy) LIKE   UPPER(CONCAT(CONCAT('%',#{condition.updateBy}),'%'))</if><if test="condition.updateTime != null" >and  UPPER(n.updateTime) LIKE   UPPER(CONCAT(CONCAT('%',#{condition.updateTime}),'%'))</if><if test="condition.version != null" >and  UPPER(n.version) LIKE   UPPER(CONCAT(CONCAT('%',#{condition.version}),'%'))</if><if test="condition.isDeleted != null" >and  UPPER(n.isDeleted) LIKE   UPPER(CONCAT(CONCAT('%',#{condition.isDeleted}),'%'))</if><if test="condition.description != null" >and  UPPER(n.description) LIKE   UPPER(CONCAT(CONCAT('%',#{condition.description}),'%'))</if></trim><include refid="inClause"/></sql><!-- 插入记录 --><insert id="add"  >  insert into news (id,title,content,createBy,createTime,updateBy,updateTime,version,isDeleted,description)  values(#{id,jdbcType=VARCHAR},#{title},#{content},#{createBy},#{createTime},#{updateBy},#{updateTime},#{version},#{isDeleted},#{description})</insert><!-- 修改记录，只修改只不为空的字段 --><update id="update" >	update news set	<trim  suffixOverrides="," >	<if test="title != null ">title=#{title},</if><if test="content != null ">content=#{content},</if><if test="createBy != null ">createBy=#{createBy},</if><if test="createTime != null ">createTime=#{createTime},</if><if test="updateBy != null ">updateBy=#{updateBy},</if><if test="updateTime != null ">updateTime=#{updateTime},</if><if test="version != null ">version=#{version},</if><if test="isDeleted != null ">isDeleted=#{isDeleted},</if><if test="description != null ">description=#{description},</if>	</trim> where id=#{id} and version = #{version}-1</update><!-- 删除记录 --><delete id="del" >	delete from news where id = #{id}</delete><!-- 根据id查询  --><select id="get"   resultType="News"  parameterType="String">	select <include refid="columnList" />	from news  n left JOIN	shiro_user u on u.id = n.createBy	where  n.id = #{id}  and n.isDeleted = false</select><!-- 列表总数--><select id="count" resultType="java.lang.Long" >	select count(1) 	from news  n left JOIN	shiro_user u on u.id = n.createBy	<include refid="whereClause"/></select><!-- 查询page --><select id="findPage" resultType="News" >	select 	<include refid="columnList"/>	from news  n left JOIN	shiro_user u on u.id = n.createBy	<include refid="whereClause"/>	<if test="orderByClause!=null">${orderByClause}</if></select><!-- 模糊查询page --><select id="findLikePage" resultType="News" >	select 	<include refid="columnList"/>	from news  n left JOIN	shiro_user u on u.id = n.createBy	<include refid="whereLikeClause"/>	<if test="orderByClause!=null">${orderByClause}</if></select><!-- 查询列表list --><select id="findList" resultType="News" >	select	<include refid="columnList"/>	from news  n left JOIN	shiro_user u on u.id = n.createBy	<include refid="whereClause"/>	<if test="orderByClause!=null">${orderByClause}</if></select><!--模糊查询list--><select id="findLikeList" resultType="News" >	select	<include refid="columnList"/>	from news  n left JOIN	shiro_user u on u.id = n.createBy	<include refid="whereLikeClause"/>	<if test="orderByClause!=null">${orderByClause}</if></select><!-- 查询符合条件的数据的id集合 --><select id="findIds" resultType="String">	select n.${idsColumn}	from news  n left JOIN	shiro_user u on u.id = n.createBy    <include refid="whereClause"/>    <if test="orderByClause!=null">${orderByClause}</if></select><!-- 查询符合模糊条件的数据的id集合 --><select id="findLikeIds" resultType="String">	select n.${idsColumn}	from news  n left JOIN	shiro_user u on u.id = n.createBy    <include refid="whereLikeClause"/>    <if test="orderByClause!=null">${orderByClause}</if></select><!-- 获取一个对象通过非id --><select id="one" resultType="News" >     select    <include refid="columnList"/>	from news  n left JOIN	shiro_user u on u.id = n.createBy    <include refid="whereClause"/>    <if test="orderByClause!=null">${orderByClause}</if>    limit 0,1</select><!--如果你想返回一个HashMap 就实现这个select标签--><select id="queryHashMap" resultType="java.util.HashMap"  parameterType="Object"></select></mapper>