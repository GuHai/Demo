<?xml version="1.0" encoding="UTF-8"?><!DOCTYPE mapper  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"  "http://mybatis.org/dtd/mybatis-3-mapper.dtd"><mapper namespace="com.yskj.dao.BankcardDao" ><!-- 查询字段 --><sql id="columnList" >	b.id,	 b.cardNumber, b.infoID, b.createTime, b.updateTime, b.version, b.isDeleted, b.createBy, b.updateBy</sql><!--通过id集合in查询--><sql id="inClause">    <if test="column!=null">        <if test="ids==null">            and 1=0        </if>        <if test="ids!=null">            <if test="ids.size>0">                and  b.${column} in                <foreach item="items" index="index" collection="ids" open="(" separator="," close=")">                    #{items}                </foreach>            </if>            <if test="ids.size==0">                and 1=0            </if>        </if>    </if></sql><!-- 查询条件 --><sql id="whereClause">where 1=1<trim  suffixOverrides="," >	<if test="condition.id != null" >	    and b.id = #{condition.id}	</if>	<if test="condition.cardNumber != null" >and  b.cardNumber = #{condition.cardNumber}</if><if test="condition.infoID != null" >and  b.infoID = #{condition.infoID}</if><if test="condition.createTime != null" >and  b.createTime = #{condition.createTime}</if><if test="condition.updateTime != null" >and  b.updateTime = #{condition.updateTime}</if><if test="condition.version != null" >and  b.version = #{condition.version}</if><if test="condition.isDeleted != null" >and  b.isDeleted = #{condition.isDeleted}</if><if test="condition.createBy != null" >and  b.createBy = #{condition.createBy}</if><if test="condition.updateBy != null" >and  b.updateBy = #{condition.updateBy}</if></trim><include refid="inClause"/></sql><!-- 模糊查询条件 --><sql id="whereLikeClause">where 1=1<trim  suffixOverrides="," >	<if test="condition.id != null" >	    and b.id = #{condition.id}	</if>													 			<if test="condition.cardNumber != null" >and  UPPER(b.cardNumber) LIKE   UPPER(CONCAT(CONCAT('%',#{condition.cardNumber}),'%'))</if><if test="condition.infoID != null" >and  UPPER(b.infoID) LIKE   UPPER(CONCAT(CONCAT('%',#{condition.infoID}),'%'))</if><if test="condition.createTime != null" >and  UPPER(b.createTime) LIKE   UPPER(CONCAT(CONCAT('%',#{condition.createTime}),'%'))</if><if test="condition.updateTime != null" >and  UPPER(b.updateTime) LIKE   UPPER(CONCAT(CONCAT('%',#{condition.updateTime}),'%'))</if><if test="condition.version != null" >and  UPPER(b.version) LIKE   UPPER(CONCAT(CONCAT('%',#{condition.version}),'%'))</if><if test="condition.isDeleted != null" >and  UPPER(b.isDeleted) LIKE   UPPER(CONCAT(CONCAT('%',#{condition.isDeleted}),'%'))</if><if test="condition.createBy != null" >and  UPPER(b.createBy) LIKE   UPPER(CONCAT(CONCAT('%',#{condition.createBy}),'%'))</if><if test="condition.updateBy != null" >and  UPPER(b.updateBy) LIKE   UPPER(CONCAT(CONCAT('%',#{condition.updateBy}),'%'))</if></trim><include refid="inClause"/></sql><!-- 插入记录 --><insert id="add"  >  insert into bankcard (id,cardNumber,infoID,createTime,updateTime,version,isDeleted,createBy,updateBy)  values(#{id,jdbcType=VARCHAR},#{cardNumber},#{infoID},#{createTime},#{updateTime},#{version},#{isDeleted},#{createBy},#{updateBy})</insert><!-- 修改记录，只修改只不为空的字段 --><update id="update" >	update bankcard set	<trim  suffixOverrides="," >	<if test="cardNumber != null ">cardNumber=#{cardNumber},</if><if test="infoID != null ">infoID=#{infoID},</if><if test="createTime != null ">createTime=#{createTime},</if><if test="updateTime != null ">updateTime=#{updateTime},</if><if test="version != null ">version=#{version},</if><if test="isDeleted != null ">isDeleted=#{isDeleted},</if><if test="createBy != null ">createBy=#{createBy},</if><if test="updateBy != null ">updateBy=#{updateBy},</if>	</trim> where id=#{id} and version = #{version}-1</update><!-- 删除记录 --><delete id="del" >	delete from bankcard where id = #{id} and version = #{version}</delete><!-- 根据id查询  --><select id="get"   resultType="Bankcard"  parameterType="String">	select <include refid="columnList" /> 	from bankcard  b	where  b.id = #{id}  and b.isDeleted = false</select><!-- 列表总数--><select id="count" resultType="java.lang.Long" >	select count(1) 	from bankcard  b	<include refid="whereClause"/></select><!-- 查询page --><select id="findPage" resultType="Bankcard" >	select 	<include refid="columnList"/>	from bankcard b	<include refid="whereClause"/>	<if test="orderByClause!=null">${orderByClause}</if></select><!-- 模糊查询page --><select id="findLikePage" resultType="Bankcard" >	select 	<include refid="columnList"/>	from bankcard b	<include refid="whereLikeClause"/>	<if test="orderByClause!=null">${orderByClause}</if></select><!-- 查询列表list --><select id="findList" resultType="Bankcard" >	select	<include refid="columnList"/>	from bankcard b	<include refid="whereClause"/>	<if test="orderByClause!=null">${orderByClause}</if></select><!--模糊查询list--><select id="findLikeList" resultType="Bankcard" >	select	<include refid="columnList"/>	from bankcard b	<include refid="whereLikeClause"/>	<if test="orderByClause!=null">${orderByClause}</if></select><!-- 查询符合条件的数据的id集合 --><select id="findIds" resultType="String">	select b.id	from bankcard b    <include refid="whereClause"/>    <if test="orderByClause!=null">${orderByClause}</if></select><!-- 查询符合模糊条件的数据的id集合 --><select id="findLikeIds" resultType="String">	select b.id	from bankcard b    <include refid="whereLikeClause"/>    <if test="orderByClause!=null">${orderByClause}</if></select><!-- 获取一个对象通过非id --><select id="one" resultType="Bankcard" >     select    <include refid="columnList"/>     from bankcard b    <include refid="whereClause"/>    <if test="orderByClause!=null">${orderByClause}</if>    limit 0,1</select><!--如果你想返回一个HashMap 就实现这个select标签--><select id="queryHashMap" resultType="java.util.HashMap"  parameterType="Object"></select></mapper>